<%= form_for(current_user.votes.build(user_id: current_user.id, photo_id: photo.id), remote: true) do |f| %>
  	<div><%= f.hidden_field :user_id %></div>
           <div><%= f.hidden_field :photo_id %></div>
  	<%= f.submit "Like", class: "btn btn-large btn-primary" %>
	<% end %>

